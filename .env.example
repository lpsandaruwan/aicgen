# GitHub Repository Configuration
# Override the default GitHub repository for guidelines

# METHOD 1: Environment Variables (highest priority)
# GitHub repository owner (default: aicgen)
AICGEN_GITHUB_OWNER=aicgen

# GitHub repository name (default: guidelines)
AICGEN_GITHUB_REPO=guidelines

# Example: To use a fork or custom repository:
# AICGEN_GITHUB_OWNER=yourname
# AICGEN_GITHUB_REPO=custom-guidelines

# METHOD 2: User Config File (alternative to env vars)
# Create ~/.aicgen/config.yml with:
#
# github:
#   owner: aicgen
#   repo: guidelines
#
# Priority: ENV > User Config > Built-in Defaults
# Most users don't need to configure anything - defaults work out of the box!
